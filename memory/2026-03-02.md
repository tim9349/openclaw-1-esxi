
- **重要資源**：OpenClaw 官方文件網站 `https://docs.openclaw.ai` (含中文 `zh-CN` 分支)。遇到 OpenClaw 相關問題 (如 CLI 指令變更、Tailscale 設定、Plugin 配置) 時，**優先查閱此網站**以獲取最新、正確的解決方案，而非盲目嘗試舊指令。
- **Tailscale 整合關鍵**：OpenClaw Gateway 需設定 `gateway.bind: "tailnet"` 才能監聽 Tailscale IP (100.x.x.x)，否則預設 Loopback 會導致 Node 無法連線。
